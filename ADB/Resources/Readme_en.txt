ADB has created a solution for custom builder "{CustomBuilderName}"

Notes£º
1.Please reconfigure the configuration for debug if you change the position of ADB.
2.If you change the name of {CustomBuilderName} or it's namespace, 
  please modify the Entry attribute of <CustomBuilder> on the file:
  Bin\Release\{CustomBuilderName}\{CustomBuilderName}.builder
